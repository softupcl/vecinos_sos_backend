export enum Role {
    USER = 'USER',
    ADMIN = 'ADMIN',
}

export enum ContactCategory {
    GENERAL = 'GENERAL',
    PERSONAL = 'PERSONAL',
}

export enum EmergencyStatus {
    ABIERTA = 'ABIERTA',
    EN_PROGRESO = 'EN_PROGRESO',
    CERRADA = 'CERRADA',
    CANCELADA = 'CANCELADA',
}
